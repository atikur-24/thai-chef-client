import React from 'react';
import TopNavbar from '../../Shared/TopNavbar';

const Header = () => {
    return (
        <header>
            <TopNavbar />
        </header>
    );
};

export default Header;